import 'package:flutter/material.dart';

class AppColors {
  // Onboarding Background Colors
  static const Color onboardingGreen = Color(0xFF5EA35F);
  static const Color onboardingTerracotta = Color(0xFFD2715A);
  static const Color onboardingAmber = Color(0xFFF7C567);

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF262626);
  static const Color textWhite = Color(0xFFFFFFFF);

  // UI Colors
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color scaffoldBackground = Color(0xFFFFFFFF); // The screenshot shows white inside the card, but the actual background seems like a very light beige/pink.
  static const Color inputBackground = Color(0xFFF3F3F3);
  static const Color inputHint = Color(0xFF262626);
  static const Color dotInactive = Color(0xFF262626);
  static const Color facebookBlue = Color(0xFF1877F2);
}
